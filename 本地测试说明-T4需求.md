# T4 需求本地测试

基线：2026-09-16 拉取的 `main`，提交 `f258e6c`。
修改分支：`codex/t4-requirements-20260916`。用户于 2026-09-16 批准推送 `main`，要求保留现用数据。

启动：在本目录运行 `node server.js 5291`，打开 http://localhost:5291，在工具箱进入 T4 日损益。
未配置门户登录的本地启动使用浏览器草稿；共享保存已另用临时数据库和测试会话验证。测试代码随仓库提交，测试产生的财务数据不会上传线上。

## 核对项目

1. 项目选择“橘农项目”，应看到天猫、淘宝、京东、抖音四个渠道。损益树、CSV、套表均包含橘农汇总。
2. 收入和成本导入吉客云销售单明细，包含“售后发货”的行应按源表金额计入零售收入/成本，0 保留为 0；退货仍按负数计入退货科目。重复导入不累计两遍。
3. “清空本期”可选择项目，以及收入、成本、运营费用或全部录入数据。输入期间号才能清空。清空收入应保留成本、其他项目及往期数据；参数和月度分摊保留。
4. 往期默认锁定，不能导入、保存或清空；点击解锁并确认后可修改，再次锁定后受保护。切换月份应显示该月份各自的数据与分摊金额。
5. “工资 / 费用分摊”可导入工资底稿、费用分摊表。先选择正确工作表并核对预览，再确认写入当前月。工资按截图中的已分摊人工和企业社保列取数，不从实发工资猜算分摊。空白保留旧值，0 写入，错误金额和未匹配渠道会阻止写入。
6. 京东玩具、快手母婴、有赞简称可匹配。电商供货保留，分销澳乐自营（零售）、（1688）分别列出。已有合并历史金额不会被猜测拆开，可在解锁后用原始明细重新导入。
7. 有赞明细导入预览提供逐行目标渠道。含“枕”的货品会提示核对，可手动选对应 zzzrest 店铺。收入、成本分别导入时，两次选择应一致。

当前渠道表下载保留原编号和销售渠道列，使用当前系统配置更新事业部、汇总渠道。上传自定义渠道映射后优先使用该映射。

## 开发验证

- `node --test tests/*.test.js`
- `python3 -m unittest discover -s tests -p 'test*.py'`
- 实际浏览器检查：CSV 导入、预览、手动渠道调整、分摊、错误值拦截、按项目清空、历史锁定、桌面和窄屏页面。
- 临时数据库集成检查：浏览器 → Node 会话代理 → SQLite 共享服务，核对月份切换、分摊独立保存、锁定及单独解锁。

## 现用数据兼容

- 保留原有渠道 ID、往期流水和旧分摊配置；已有共享工作区不会被本机草稿覆盖。
- 读取已有共享工作区不触发保存；编辑本月分摊不改变其他月份数据或旧配置。
- 服务端沿用现有 SQLite 表结构、版本控制和修订历史，无需清库或执行数据库结构迁移。
- 推送仅包含代码、测试和说明，不包含数据库、运行数据或凭据文件；不执行线上数据操作。
