# 财务中心登录与渠道保存

更新于 2026-09-20。本文替代早期仅跳转、无鉴权的接入说明。

## 同事如何使用

1. 打开 `https://finance.vvaix.com`。
2. 若显示“未登录”，点击“登录财务中心”。
3. 在门户登录，然后点击“财务中心”卡片，系统会回到原网址。
4. 在 T4 渠道列表确认“已连接财务中心，可保存”，下载当前列表、修改 xlsx 后重新导入。

页面能打开不代表已经登录。右上角身份来自当前会话，不再显示固定姓名。渠道导入成功后保存到财务中心数据库，其他同事重新加载后可见；Git 仓库用于部署代码，不用于保存每次业务导入。

已有渠道未出现在新文件中时会保留，历史损益不会因渠道导入而删除。

## 当前生产链路

- 财务中心：`https://finance.vvaix.com`
- 门户工作台：`https://portal.vvaix.com/apps`
- 财务中心系统编码与 AppID：`finance`
- 门户 SSO 协议：`1`，启用 SSO：`1`
- 回调地址：`https://finance.vvaix.com/sso/callback`
- 门户以 auth_code 回调；财务中心服务端向门户 `/v1/sso/verify` 校验，成功后建立 HttpOnly 会话。
- 会话有效期为 8 小时。过期后按上述入口重新登录；页面会提示登录，不会把未保存的修改说成已保存。

门户已有财务中心系统登记和授权关联。是否允许某位同事进入，继续由门户现有账号及系统开通权限决定。不要重复注册系统或为了消除 401 开放匿名写入。

## 服务端环境

| 变量 | 用途 |
|---|---|
| `T4_PORTAL_SSO_BASE` | 门户服务端 API 基址，包含 `/api`；服务端追加 `/v1/sso/verify` |
| `T4_PORTAL_PUBLIC_URL` | 用户浏览器可访问的门户工作台，默认 `https://portal.vvaix.com/apps` |
| `T4_SESSION_SECRET` | 财务中心会话签名密钥，只存服务器 |
| `T4_PROXY_SECRET` | 财务中心与内部数据服务间的签名密钥，只存服务器 |
| `T4_SYNC_HOST` / `T4_SYNC_PORT` | 内部数据服务地址 |

`/api/session` 只返回当前浏览器的登录状态和显示姓名，不返回令牌。`/sso/login` 跳转到配置的门户工作台；当前门户在用户点击财务中心卡片后签票，单独打开工作台还没有完成财务中心登录。

## 排查顺序

- `401`：未登录或会话过期，使用页面上的登录入口。
- `503` 或连接失败：检查财务中心和内部数据服务状态，恢复后在原页面重试。
- 页面显示已连接、导入仍失败：按页面具体错误检查文件、月份锁定或多人修改冲突。
- 修改后另一位同事看不到：确认两边均已登录，导入成功后让另一位同事刷新页面。

健康检查 `/healthz` 只证明服务在线。发布还应运行登录回调、真实数据服务、xlsx 导入及两个浏览器上下文读取的测试。
